include:
  - project: ulysse_durand/gitlabci-templates
    file: templates/str2str-api.yml

stages:
  - build

build-ocamlrun:
  stage: build
  extends: .str2str-api-build
  variables:
    STR2STR_DIR: .
    STR2STR_IMAGENAME: str2str
    API_IMAGENAME: api